# Logistic Regression (Логістична регресія)

## Що це?

**Логістична регресія** — це алгоритм supervised learning для **бінарної класифікації**, який передбачає **ймовірність** належності об'єкта до певного класу.

**Головна ідея:** перетворити лінійну комбінацію ознак у ймовірність $P(y=1|X)$ за допомогою **сигмоїдної функції** (логістичної функції).

## Навіщо потрібна?

- 🎯 **Класифікація** — спам/не спам, хвороба/здоровий, схвалити/відхилити
- 📊 **Ймовірності** — не просто клас, а ймовірність належності
- 🔍 **Інтерпретація** — зрозуміти вплив ознак через odds ratio
- ⚡ **Швидкість** — швидке навчання та передбачення
- 📈 **Baseline** — стартова модель для класифікації

## Коли використовувати?

**Потрібно:**
- **Бінарна класифікація** (2 класи: 0/1, Yes/No)
- Класи **лінійно розділимі** або близько до цього
- Потрібні **калібровані ймовірності** $P(y=1)$
- **Інтерпретованість** важлива
- Швидке навчання на великих даних

**Не потрібно:**
- Багатокласова класифікація → Softmax Regression, Tree-based
- Класи **нелінійно розділимі** → SVM з kernel, Neural Networks
- Лише передбачення класу без ймовірностей → SVM
- Регресія (числова змінна) → Linear Regression

---

## Математика

### Лінійна комбінація (logit)

$$z = \beta_0 + \beta_1 x_1 + \beta_2 x_2 + ... + \beta_p x_p = \beta^T X$$

де:
- $z$ — log-odds (логарифм шансів)
- $\beta$ — коефіцієнти моделі
- $X$ — вектор ознак

### Сигмоїдна функція

$$\sigma(z) = \frac{1}{1 + e^{-z}}$$

**Властивості:**
- Діапазон: $(0, 1)$ — ідеально для ймовірностей
- Монотонно зростає
- S-подібна крива
- $\sigma(0) = 0.5$
- $\sigma(-\infty) = 0$, $\sigma(+\infty) = 1$

### Передбачення ймовірності

$$P(y=1|X) = \sigma(\beta^T X) = \frac{1}{1 + e^{-\beta^T X}}$$

$$P(y=0|X) = 1 - P(y=1|X) = \frac{e^{-\beta^T X}}{1 + e^{-\beta^T X}}$$

### Передбачення класу (Decision Boundary)

$$\hat{y} = \begin{cases} 
1 & \text{якщо } P(y=1|X) \geq 0.5 \\
0 & \text{якщо } P(y=1|X) < 0.5
\end{cases}$$

---

## Графік сигмоїди

```
P(y=1)
  1 |           ────────
    |         ╱
0.5 |       ╱     ← Decision boundary (z=0)
    |     ╱
  0 | ───
    |_____________________
       -∞    0    +∞    z
```

**Інтерпретація:**
- $z > 0$ → $P(y=1) > 0.5$ → клас 1
- $z = 0$ → $P(y=1) = 0.5$ → межа рішення
- $z < 0$ → $P(y=1) < 0.5$ → клас 0

---

## Простий приклад: Схвалення кредиту

### Дані

| Дохід (тис. $) | Кредитний рейтинг | Схвалено |
|----------------|-------------------|----------|
| 30 | 400 | 0 (Ні) |
| 50 | 600 | 1 (Так) |
| 40 | 550 | 0 (Ні) |
| 70 | 750 | 1 (Так) |
| 60 | 700 | 1 (Так) |
| 35 | 450 | 0 (Ні) |
| 80 | 800 | 1 (Так) |

### Модель

Після навчання отримали коефіцієнти:
$$z = -5 + 0.05 \times \text{Дохід} + 0.01 \times \text{Рейтинг}$$

### Передбачення для нового клієнта

**Клієнт:** Дохід = 55 тис. $, Рейтинг = 650

**Крок 1:** Обчислити log-odds
$$z = -5 + 0.05(55) + 0.01(650)$$
$$z = -5 + 2.75 + 6.5 = 4.25$$

**Крок 2:** Застосувати sigmoid
$$P(\text{Схвалено}=1) = \frac{1}{1 + e^{-4.25}} = \frac{1}{1 + 0.014} = 0.986$$

**Крок 3:** Прийняти рішення (threshold = 0.5)
$$P = 0.986 > 0.5 \Rightarrow \text{Схвалено!} \checkmark$$

**Інтерпретація:** Ймовірність схвалення кредиту — **98.6%**

---

## Складний приклад: Діагностика діабету

### Дані

1000 пацієнтів:

| Ознака | Діапазон | Опис |
|--------|----------|------|
| Glucose | 70-200 mg/dL | Рівень глюкози |
| BMI | 18-45 | Індекс маси тіла |
| Age | 21-81 | Вік |
| BloodPressure | 60-120 | Тиск |
| **Outcome** | 0/1 | Діабет (0=Ні, 1=Так) |

**Розподіл:** 65% здорові, 35% хворі (незбалансовані класи)

### Навчена модель

$$z = -8 + 0.05 \times \text{Glucose} + 0.1 \times \text{BMI} + 0.02 \times \text{Age} + 0.01 \times \text{BP}$$

### Приклад 1: Молода людина з нормальними показниками

**Дані:** Glucose=90, BMI=22, Age=25, BP=80

$$z = -8 + 0.05(90) + 0.1(22) + 0.02(25) + 0.01(80)$$
$$z = -8 + 4.5 + 2.2 + 0.5 + 0.8 = 0$$

$$P(\text{Діабет}=1) = \frac{1}{1 + e^{0}} = 0.5$$

**Рішення:** **Граничний випадок** (потрібні додаткові обстеження)

### Приклад 2: Людина з високими показниками

**Дані:** Glucose=180, BMI=35, Age=55, BP=110

$$z = -8 + 0.05(180) + 0.1(35) + 0.02(55) + 0.01(110)$$
$$z = -8 + 9 + 3.5 + 1.1 + 1.1 = 6.7$$

$$P(\text{Діабет}=1) = \frac{1}{1 + e^{-6.7}} = 0.999$$

**Рішення:** **Діабет** (ймовірність 99.9%) ⚠️

### Приклад 3: Проміжний випадок

**Дані:** Glucose=120, BMI=28, Age=45, BP=90

$$z = -8 + 0.05(120) + 0.1(28) + 0.02(45) + 0.01(90)$$
$$z = -8 + 6 + 2.8 + 0.9 + 0.9 = 2.6$$

$$P(\text{Діабет}=1) = \frac{1}{1 + e^{-2.6}} = 0.931$$

**Рішення:** **Висока ймовірність діабету** (93.1%)

---

## Функція втрат: Log-Loss (Binary Cross-Entropy)

### Для одного прикладу

$$L(y, \hat{p}) = -[y \log(\hat{p}) + (1-y) \log(1-\hat{p})]$$

де:
- $y \in \{0, 1\}$ — реальний клас
- $\hat{p} = P(y=1|X)$ — передбачена ймовірність

### Для всього датасету

$$J(\beta) = -\frac{1}{n} \sum_{i=1}^{n} [y_i \log(\hat{p}_i) + (1-y_i) \log(1-\hat{p}_i)]$$

### Інтуїція

**Якщо реальний клас $y=1$:**
- $\hat{p} = 0.9$ → $L = -\log(0.9) = 0.105$ (мала помилка) ✓
- $\hat{p} = 0.5$ → $L = -\log(0.5) = 0.693$ (помірна помилка)
- $\hat{p} = 0.1$ → $L = -\log(0.1) = 2.303$ (велика помилка) ✗

**Якщо реальний клас $y=0$:**
- $\hat{p} = 0.1$ → $L = -\log(0.9) = 0.105$ (мала помилка) ✓
- $\hat{p} = 0.5$ → $L = -\log(0.5) = 0.693$ (помірна помилка)
- $\hat{p} = 0.9$ → $L = -\log(0.1) = 2.303$ (велика помилка) ✗

> **Log-loss суворо карає впевнені, але неправильні передбачення!**

---

## Оптимізація: Gradient Descent

Немає аналітичного рішення для Logistic Regression → використовуємо градієнтний спуск.

### Градієнт

$$\frac{\partial J}{\partial \beta_j} = \frac{1}{n} \sum_{i=1}^{n} (\hat{p}_i - y_i) x_{ij}$$

де $\hat{p}_i = \sigma(\beta^T X_i)$.

### Оновлення параметрів

$$\beta_j := \beta_j - \alpha \frac{\partial J}{\partial \beta_j}$$

де $\alpha$ — learning rate.

### Порівняння з Linear Regression

| | Linear Regression | Logistic Regression |
|---|---|---|
| **Передбачення** | $\hat{y} = \beta^T X$ | $\hat{p} = \sigma(\beta^T X)$ |
| **Loss** | MSE | Log-Loss |
| **Градієнт** | $\frac{1}{n} \sum (\hat{y}_i - y_i) x_{ij}$ | $\frac{1}{n} \sum (\hat{p}_i - y_i) x_{ij}$ |
| **Рішення** | Аналітичне (Normal Eq) | Gradient Descent |

---

## Інтерпретація: Odds Ratio

### Шанси (Odds)

$$\text{Odds} = \frac{P(y=1)}{P(y=0)} = \frac{P(y=1)}{1 - P(y=1)}$$

**Приклад:**
- $P(y=1) = 0.8$ → Odds = $\frac{0.8}{0.2} = 4$ (шанси 4:1 на користь класу 1)

### Логарифм шансів (Log-Odds)

$$\log(\text{Odds}) = \log\left(\frac{P(y=1)}{1-P(y=1)}\right) = \beta^T X = z$$

**Висновок:** Logistic Regression моделює **log-odds як лінійну функцію від X**.

### Odds Ratio (OR)

При збільшенні $x_j$ на 1 одиницю:
$$\text{Odds Ratio} = e^{\beta_j}$$

**Інтерпретація:**

| $\beta_j$ | $e^{\beta_j}$ | Значення |
|-----------|---------------|----------|
| 0.69 | 2.0 | Шанси **подвоюються** |
| 1.10 | 3.0 | Шанси **потроюються** |
| -0.69 | 0.5 | Шанси **зменшуються вдвічі** |
| 0 | 1.0 | Ознака **не впливає** |

### Приклад

У моделі діагностики діабету: $\beta_{\text{Glucose}} = 0.05$

$$\text{OR} = e^{0.05} = 1.051$$

**Інтерпретація:** При збільшенні глюкози на 1 mg/dL, шанси мати діабет зростають у **1.051 рази** (або на 5.1%).

---

## Decision Threshold (Поріг рішення)

### Стандартний поріг: 0.5

$$\hat{y} = \begin{cases}
1 & \text{якщо } P(y=1) \geq 0.5 \\
0 & \text{якщо } P(y=1) < 0.5
\end{cases}$$

### Налаштування порогу

**Приклад: діагностика раку**

| Поріг | Precision | Recall | Використання |
|-------|-----------|--------|--------------|
| 0.3 | Низька (60%) | Висока (95%) | Критично не пропустити хворого |
| 0.5 | Середня (75%) | Середній (80%) | Баланс |
| 0.7 | Висока (90%) | Низький (70%) | Важливо уникнути false positives |

### Trade-off

```
Поріг 0.3:
✓ Recall: 95% — майже всіх хворих знайшли
✗ Precision: 60% — багато false positives

Поріг 0.7:
✗ Recall: 70% — пропустили деяких хворих
✓ Precision: 90% — майже всі позитиви правильні
```

---

## Метрики оцінки

### Confusion Matrix

```
                Передбачено
                 0       1
Реально  0    TN=50   FP=10   
         1    FN=5    TP=35   
```

### Основні метрики

| Метрика | Формула | Значення |
|---------|---------|----------|
| **Accuracy** | $\frac{TP + TN}{TP + TN + FP + FN}$ | Загальна точність |
| **Precision** | $\frac{TP}{TP + FP}$ | Скільки з позитивних правильні |
| **Recall (Sensitivity)** | $\frac{TP}{TP + FN}$ | Скільки реальних позитивних знайшли |
| **Specificity** | $\frac{TN}{TN + FP}$ | Скільки негативних правильно визначили |
| **F1-Score** | $2 \times \frac{\text{Precision} \times \text{Recall}}{\text{Precision} + \text{Recall}}$ | Гармонійне середнє |

### Приклад розрахунку

Для матриці вище:
- **Accuracy** = $\frac{50+35}{100} = 0.85$ (85%)
- **Precision** = $\frac{35}{35+10} = 0.78$ (78%)
- **Recall** = $\frac{35}{35+5} = 0.88$ (88%)
- **F1-Score** = $2 \times \frac{0.78 \times 0.88}{0.78 + 0.88} = 0.82$ (82%)

### ROC Curve та AUC

**ROC (Receiver Operating Characteristic)** — графік TPR vs FPR при різних порогах:

```
TPR (Recall)
  1 |    ╱────  ← Ідеальна модель (AUC=1.0)
    |   ╱
    |  ╱  ← Добра модель (AUC~0.9)
0.5 | ╱
    |╱_____ ← Випадкове гадання (AUC=0.5)
  0 |________
    0   0.5  1  FPR
```

**AUC (Area Under Curve):**
- AUC = 1.0 → Ідеальна модель
- AUC = 0.9-1.0 → Відмінна
- AUC = 0.8-0.9 → Добра
- AUC = 0.7-0.8 → Прийнятна
- AUC = 0.5 → Як підкидання монети

---

## Код (Python + scikit-learn)

### Простий приклад

```python
import numpy as np
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import accuracy_score, classification_report

# Дані
X = np.array([[30, 400], [50, 600], [40, 550], 
              [70, 750], [60, 700], [35, 450], [80, 800]])
y = np.array([0, 1, 0, 1, 1, 0, 1])

# Модель
model = LogisticRegression()
model.fit(X, y)

# Коефіцієнти
print(f"Intercept (β₀): {model.intercept_[0]:.4f}")
print(f"Coefficients (β): {model.coef_[0]}")

# Передбачення класу
X_new = np.array([[55, 650]])
y_pred_class = model.predict(X_new)
print(f"Predicted class: {y_pred_class[0]}")  # 1

# Передбачення ймовірності
y_pred_proba = model.predict_proba(X_new)
print(f"P(class=0): {y_pred_proba[0][0]:.4f}")
print(f"P(class=1): {y_pred_proba[0][1]:.4f}")  # 0.986

# Оцінка
y_pred = model.predict(X)
print(f"Accuracy: {accuracy_score(y, y_pred):.4f}")
```

### Повний приклад

```python
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import (
    classification_report, 
    confusion_matrix,
    roc_auc_score,
    roc_curve
)
import matplotlib.pyplot as plt

# Генерація даних
from sklearn.datasets import make_classification
X, y = make_classification(
    n_samples=1000,
    n_features=10,
    n_informative=5,
    n_redundant=2,
    n_classes=2,
    weights=[0.65, 0.35],  # Незбалансовані класи
    random_state=42
)

# Розділення
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.2, stratify=y, random_state=42
)

# Нормалізація (ВАЖЛИВО для LogReg!)
scaler = StandardScaler()
X_train_scaled = scaler.fit_transform(X_train)
X_test_scaled = scaler.transform(X_test)

# Модель
model = LogisticRegression(
    penalty='l2',          # Ridge regularization
    C=1.0,                 # Inverse of λ
    solver='lbfgs',        # Optimization algorithm
    max_iter=1000,
    random_state=42
)

model.fit(X_train_scaled, y_train)

# Передбачення
y_pred = model.predict(X_test_scaled)
y_pred_proba = model.predict_proba(X_test_scaled)[:, 1]

# Метрики
print("=== Classification Report ===")
print(classification_report(y_test, y_pred))

print("\n=== Confusion Matrix ===")
cm = confusion_matrix(y_test, y_pred)
print(cm)

print(f"\nAccuracy: {accuracy_score(y_test, y_pred):.4f}")
print(f"ROC-AUC: {roc_auc_score(y_test, y_pred_proba):.4f}")

# Коефіцієнти та Odds Ratios
print("\n=== Coefficients & Odds Ratios ===")
print(f"Intercept: {model.intercept_[0]:.4f}")
for i, coef in enumerate(model.coef_[0]):
    odds_ratio = np.exp(coef)
    print(f"Feature {i}: β={coef:.4f}, OR={odds_ratio:.4f}")
```

### ROC Curve

```python
# ROC Curve
fpr, tpr, thresholds = roc_curve(y_test, y_pred_proba)
auc = roc_auc_score(y_test, y_pred_proba)

plt.figure(figsize=(10, 6))
plt.plot(fpr, tpr, linewidth=2, 
         label=f'ROC Curve (AUC={auc:.3f})')
plt.plot([0, 1], [0, 1], 'k--', linewidth=2, 
         label='Random Guess (AUC=0.5)')
plt.xlabel('False Positive Rate', fontsize=12)
plt.ylabel('True Positive Rate (Recall)', fontsize=12)
plt.title('ROC Curve', fontsize=14, fontweight='bold')
plt.legend(fontsize=11)
plt.grid(True, alpha=0.3)
plt.tight_layout()
plt.show()
```

### Вибір порогу

```python
# Precision-Recall для різних порогів
from sklearn.metrics import precision_recall_curve

precision, recall, thresholds = precision_recall_curve(
    y_test, y_pred_proba
)

# Візуалізація
plt.figure(figsize=(10, 6))
plt.plot(thresholds, precision[:-1], label='Precision')
plt.plot(thresholds, recall[:-1], label='Recall')
plt.axvline(x=0.5, color='r', linestyle='--', 
            label='Default threshold (0.5)')
plt.xlabel('Threshold', fontsize=12)
plt.ylabel('Score', fontsize=12)
plt.title('Precision-Recall vs Threshold', 
          fontsize=14, fontweight='bold')
plt.legend(fontsize=11)
plt.grid(True, alpha=0.3)
plt.tight_layout()
plt.show()

# Вибір кастомного порогу
custom_threshold = 0.3
y_pred_custom = (y_pred_proba >= custom_threshold).astype(int)
print(f"\nWith threshold={custom_threshold}:")
print(classification_report(y_test, y_pred_custom))
```

---

## Багатокласова класифікація

### One-vs-Rest (OvR)

Для K класів будуємо K бінарних класифікаторів:
- Класифікатор 1: "Клас 1" vs "Інші"
- Класифікатор 2: "Клас 2" vs "Інші"
- ...

**Передбачення:** клас з найвищою ймовірністю.

```python
model = LogisticRegression(multi_class='ovr')
model.fit(X_train, y_train)
```

### Softmax Regression (Multinomial)

Узагальнення для K класів:

$$P(y=k|X) = \frac{e^{z_k}}{\sum_{j=1}^{K} e^{z_j}}$$

де $z_k = \beta_k^T X$.

```python
model = LogisticRegression(multi_class='multinomial')
model.fit(X_train, y_train)
```

---

## Регуляризація

### L1 (Lasso)

$$J(\beta) = -\frac{1}{n} \sum \text{log-loss} + \lambda \sum_{j=1}^{p} |\beta_j|$$

- Зануляє непотрібні коефіцієнти
- **Feature selection**

```python
model = LogisticRegression(penalty='l1', solver='liblinear')
```

### L2 (Ridge)

$$J(\beta) = -\frac{1}{n} \sum \text{log-loss} + \lambda \sum_{j=1}^{p} \beta_j^2$$

- Зменшує величину коефіцієнтів
- Допомагає при мультиколінеарності

```python
model = LogisticRegression(penalty='l2', C=1.0)  # C = 1/λ
```

### Elastic Net

$$J(\beta) = \text{log-loss} + \lambda [\alpha \sum |\beta_j| + (1-\alpha) \sum \beta_j^2]$$

```python
model = LogisticRegression(penalty='elasticnet', 
                          solver='saga', 
                          l1_ratio=0.5)
```

---

## Переваги та недоліки

### Переваги ✓

| Перевага | Пояснення |
|----------|-----------|
| **Ймовірності** | Дає калібровані P(y=1), не тільки клас |
| **Інтерпретованість** | Odds ratio має чіткий зміст |
| **Швидкість** | Швидке навчання та передбачення |
| **Regularization** | Легко додати L1/L2 |
| **Малі дані** | Працює на невеликих вибірках |
| **Baseline** | Відмінна стартова модель |
| **Багатокласовість** | Легко розширити (OvR, Softmax) |

### Недоліки ✗

| Недолік | Пояснення |
|---------|-----------|
| **Лінійна межа** | Передбачає лінійну розділимість |
| **Нелінійні залежності** | Погано з складними взаємодіями |
| **Чутливість до outliers** | Екстремуми впливають на коефіцієнти |
| **Мультиколінеарність** | Проблеми при сильній кореляції |
| **Незбалансовані класи** | Потребує спеціальної обробки |

---

## Обробка незбалансованих класів

### Проблема

Якщо 95% клас 0, модель може завжди передбачати 0 → accuracy=95%, але безкорисна!

### Рішення

#### 1. Class weights

```python
model = LogisticRegression(class_weight='balanced')
# Автоматично підбирає ваги пропорційно до частот класів
```

#### 2. Custom weights

```python
model = LogisticRegression(class_weight={0: 1, 1: 5})
# Клас 1 в 5 разів важливіший
```

#### 3. Resampling

```python
from imblearn.over_sampling import SMOTE
from imblearn.under_sampling import RandomUnderSampler

# Oversampling minority class
smote = SMOTE(random_state=42)
X_resampled, y_resampled = smote.fit_resample(X_train, y_train)

# Undersampling majority class
rus = RandomUnderSampler(random_state=42)
X_resampled, y_resampled = rus.fit_resample(X_train, y_train)
```

#### 4. Adjust threshold

```python
# Знизити поріг для рідкісного класу
threshold = 0.3  # Замість 0.5
y_pred = (y_pred_proba >= threshold).astype(int)
```

#### 5. Використовувати правильні метрики

```python
# ❌ НЕ використовувати: Accuracy
# ✅ Використовувати: F1, Precision, Recall, ROC-AUC
```

---

## Коли використовувати Logistic Regression

### Ідеально підходить ✓

- **Бінарна класифікація** з лінійно розділимими класами
- Потрібні **ймовірності**, не тільки класи
- **Інтерпретованість** важлива (медицина, фінанси)
- **Швидкий baseline** перед складнішими моделями
- Малі/середні датасети

### Краще використати інше ✗

- Класи **нелінійно розділимі** → SVM з kernel, Tree-based, Neural Networks
- **Складні взаємодії** → Random Forest, Gradient Boosting
- Багато **outliers** → Robust models
- **Зображення, текст** → Deep Learning
- Тільки передбачення, без ймовірностей → SVM

---

## Практичні поради 💡

1. **Завжди нормалізуй дані** — StandardScaler перед навчанням
2. **Використовуй stratify** при розділенні незбалансованих класів
3. **ROC-AUC краще за Accuracy** — особливо для незбалансованих даних
4. **Підбирай threshold** — 0.5 не завжди оптимальний
5. **Regularization завжди** — L2 за замовчуванням, L1 для feature selection
6. **Class weights** для незбалансованих класів
7. **Інтерпретуй через Odds Ratio** — exp(β) має практичний зміст
8. **Cross-validation** для надійної оцінки
9. **Перевіряй на overfitting** — train vs test metrics
10. **Feature engineering** — polynomial, interactions можуть допомогти

---

## Пов'язані теми

- [[01_Linear_Regression]] — для регресії
- [[03_Regularization]] — Ridge, Lasso, Elastic Net
- [[Decision_Trees]] — для нелінійних меж
- [[SVM]] — для складних меж рішення
- [[Cross_Validation]] — оцінка моделей
- [[Feature_Engineering]] — покращення ознак

## Ресурси

- [Scikit-learn: Logistic Regression](https://scikit-learn.org/stable/modules/linear_model.html#logistic-regression)
- [StatQuest: Logistic Regression](https://www.youtube.com/watch?v=yIYKR4sgzI8)
- [Andrew Ng: Classification](https://www.coursera.org/learn/machine-learning)

---

## Ключові висновки

> Logistic Regression перетворює лінійну комбінацію ознак у ймовірність за допомогою сигмоїдної функції.

**Основні принципи:**
- Використовує sigmoid: $\sigma(z) = \frac{1}{1 + e^{-z}}$
- Оптимізує log-loss функцію
- Дає калібровані ймовірності $P(y=1|X)$
- Коефіцієнти інтерпретуються через odds ratio: $\text{OR} = e^{\beta}$
- Threshold можна налаштовувати під задачу

**Формула передбачення:**
$$P(y=1|X) = \frac{1}{1 + e^{-(\beta_0 + \beta_1 x_1 + ... + \beta_p x_p)}}$$

**Коли використовувати:**
- Бінарна класифікація + інтерпретованість + ймовірності = Logistic Regression ✓

---

#ml #supervised-learning #classification #logistic-regression #binary-classification
